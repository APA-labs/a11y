export const breadcrumbCss = `.breadcrumb { display: flex; align-items: center; gap: 6px; font-size: 13px; list-style: none; padding: 0; margin: 0; flex-wrap: wrap; }
.breadcrumb-sep { color: #9ca3af; }`
