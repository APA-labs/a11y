# Shared

`src/types/analysis.ts` defines `AnalysisRequest`, `AnalysisResponse`, `ChecklistItem`, etc. Import from `@a11y/shared` in both frontend and backend. Zod schemas live alongside the types in `src/schemas/`.
